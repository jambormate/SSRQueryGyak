<%- include('header') %>
<h2>Drága kiadások</h2>
<form action="/expensive" method="get">
    <label for="amount">Keresés ár alapján:</label>
    <input
        type="number"
        id="amount"
        name="amount"
        value="<%= amount %>"
    >
    <button type="submit">Keresés</button>
</form>
<% if (expenses.length === 0) { %>
    <p>Nincs találat</p>
<% } else { %>
    <table>
        <thead>
            <tr>
                <th>Megnevezés</th>
                <th>Összeg</th>
                <th>Kategória</th>
            </tr>
        </thead>
        <tbody>
            <% expenses.forEach(expense => { %>
                <tr class="<%= expense.category %>">
                    <td><%= expense.name %></td>
                    <td><%= expense.amount %> Ft</td>
                    <td><%= expense.category %></td>
                </tr>
            <% }) %>
        </tbody>
    </table>
<% } %>
<%- include('footer') %>